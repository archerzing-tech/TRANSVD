import { useState, useCallback, useEffect } from "react";import type { VideoFile } from "../../App";import { useFFmpeg } from "../../hooks/useFFmpeg";import { useTranslation } from "../../context/LanguageContext";import ProcessingOverlay from "../common/ProcessingOverlay";
import DownloadButton from "../common/DownloadButton";

interface ResizePanelProps {  video: VideoFile;}const PRESETS = [  { label: "4K (3840x2160)", w: 3840, h: 2160 },  { label: "1440p (2560x1440)", w: 2560, h: 1440 },  { label: "1080p (1920x1080)", w: 1920, h: 1080 },  { label: "720p (1280x720)", w: 1280, h: 720 },  { label: "480p (854x480)", w: 854, h: 480 },  { label: "360p (640x360)", w: 640, h: 360 },  { label: "240p (426x240)", w: 426, h: 240 },];export default function ResizePanel({ video }: ResizePanelProps) {  const { init, run, cancel, progress, log, loaded, loading, error, cancelling, running } = useFFmpeg();  const { t } = useTranslation();  const [width, setWidth] = useState(1280);  const [height, setHeight] = useState(720);  const [maintainAspect, setMaintainAspect] = useState(true);  const [outputUrl, setOutputUrl] = useState<string | null>(null);
  const [outputBlob, setOutputBlob] = useState<Blob | null>(null);  useEffect(() => { init(); }, [init]);  const applyPreset = (p: typeof PRESETS[0]) => {    setWidth(p.w);    setHeight(p.h);  };  const handleResize = useCallback(async () => {    setOutputUrl(null);    await run(async (instance) => {      if (!video.data) throw new Error("No video data loaded");      const ext = video.name.match(/\.[^.]+$/)?.[0] || ".mp4";      const inputName = "input" + ext;      const outputName = "resized" + ext;      await instance.writeFile(inputName, video.data);      const scale = maintainAspect ? `scale=${width}:${height}:force_original_aspect_ratio=decrease,pad=${width}:${height}:(ow-iw)/2:(oh-ih)/2` : `scale=${width}:${height}`;      await instance.exec([        "-i", inputName,        "-vf", scale,        "-c:v", "libx264",        "-c:a", "aac",        "-y", outputName,      ]);      const raw = await instance.readFile(outputName);      const blob = new Blob([raw as BlobPart], { type: "video/mp4" });      setOutputBlob(blob);
      setOutputUrl(URL.createObjectURL(blob));
    });
  }, [video.data, video.name, width, height, maintainAspect, run]);
  return (    <div className="card space-y-6">      <div>        <label className="block text-sm text-surface-400 mb-2">{t("resize.presets")}</label>        <div className="flex flex-wrap gap-2">          {PRESETS.map((p) => (            <button key={p.label} onClick={() => applyPreset(p)}              className={`px-3 py-1.5 text-xs rounded-lg transition-colors ${width === p.w ? "bg-brand-600 text-white" : "bg-surface-800 text-surface-300 hover:bg-surface-700"}`}>              {p.label}            </button>          ))}        </div>      </div>      <div className="grid grid-cols-2 gap-4">        <div>          <label className="block text-sm text-surface-400 mb-1">{t("resize.width")}</label>          <input type="number" min={64} max={7680} step={2} value={width}            onChange={(e) => setWidth(Number(e.target.value))} className="select-input w-full" />        </div>        <div>          <label className="block text-sm text-surface-400 mb-1">{t("resize.height")}</label>          <input type="number" min={64} max={4320} step={2} value={height}            onChange={(e) => setHeight(Number(e.target.value))} className="select-input w-full" />        </div>      </div>      <label className="flex items-center gap-2 text-sm text-surface-400">        <input type="checkbox" checked={maintainAspect} onChange={(e) => setMaintainAspect(e.target.checked)}          className="rounded bg-surface-800 border-surface-700" />        {t("resize.maintain")}      </label>      <ProcessingOverlay active={running} progress={progress} label={t("resize.resizing")} log={log} onCancel={cancel} cancelling={cancelling} />      {error && <div className="banner-error">{error}</div>}      <div className="flex gap-3">        <button onClick={handleResize} disabled={loading || running} className="btn-primary">          {!loaded ? t("common.load_ffmpeg") : t("resize.resize")}        </button>        {outputUrl && outputBlob && <DownloadButton blob={outputBlob} filename={video.name.replace(/\.[^.]+$/, "") + `_${width}x${height}.mp4`} filters={[{ name: "MP4 Video", extensions: ["mp4"] }]} label={t("common.download")} />}      </div>    </div>  );}