import { useState, useCallback, useEffect } from "react";import type { VideoFile } from "../../App";import { useFFmpeg } from "../../hooks/useFFmpeg";import { useTranslation } from "../../context/LanguageContext";import ProcessingOverlay from "../common/ProcessingOverlay";interface TrimPanelProps {  video: VideoFile;}export default function TrimPanel({ video }: TrimPanelProps) {  const { init, run, cancel, progress, log, loaded, loading, error, cancelling } = useFFmpeg();  const { t } = useTranslation();  const [start, setStart] = useState(0);  const [end, setEnd] = useState(10);  const [outputUrl, setOutputUrl] = useState<string | null>(null);  useEffect(() => { init(); }, [init]);  const handleTrim = useCallback(async () => {    setOutputUrl(null);    const duration = end - start;    if (duration <= 0) return;    await run(async (instance) => {      if (!video.data) throw new Error("No video data loaded");      const ext = video.name.match(/\.[^.]+$/)?.[0] || ".mp4";      const inputName = "input" + ext;      const outputName = "trimmed" + ext;      await instance.writeFile(inputName, video.data);      await instance.exec([        "-ss", String(start),        "-t", String(duration),        "-i", inputName,        "-c", "copy",        "-y", outputName,      ]);      const raw = await instance.readFile(outputName);      const blob = new Blob([raw as BlobPart], { type: "video/mp4" });      setOutputUrl(URL.createObjectURL(blob));    });  }, [video.data, video.name, start, end, run]);
  return (    <div className="card space-y-6">      <div className="grid grid-cols-2 gap-4">        <div>          <label className="block text-sm text-surface-400 mb-1">{t("trim.start")}: {start}s</label>          <input type="range" min={0} max={Math.max(end - 1, 60)} step={0.5} value={start}            onChange={(e) => setStart(Number(e.target.value))} className="input-range" />        </div>        <div>          <label className="block text-sm text-surface-400 mb-1">{t("trim.end")}: {end}s</label>          <input type="range" min={start + 1} max={300} step={0.5} value={end}            onChange={(e) => setEnd(Number(e.target.value))} className="input-range" />        </div>      </div>      <p className="text-sm text-surface-500 text-center">{t("trim.duration")}: {(end - start).toFixed(1)}s</p>      <ProcessingOverlay active={progress > 0} progress={progress} label={t("trim.trimming")} log={log} onCancel={cancel} cancelling={cancelling} />      {error && <div className="banner-error">{error}</div>}      <div className="flex gap-3">        <button onClick={handleTrim} disabled={loading || (progress > 0 && progress < 100)} className="btn-primary">          {!loaded ? t("common.load_ffmpeg") : t("trim.trim")}        </button>        {outputUrl && <a href={outputUrl} download={`${video.name.replace(/\.[^.]+$/, "")}_trimmed.mp4`} className="btn-secondary">{t("common.download")}</a>}      </div>    </div>  );}