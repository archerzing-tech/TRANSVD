import { useState, useCallback, useEffect } from "react";import type { VideoFile } from "../../App";import { useFFmpeg } from "../../hooks/useFFmpeg";import { useTranslation } from "../../context/LanguageContext";import ProcessingOverlay from "../common/ProcessingOverlay";
import DownloadButton from "../common/DownloadButton";

interface AudioExtractPanelProps {  video: VideoFile;}const FORMATS = [  { ext: "mp3", mime: "audio/mpeg", label: "MP3", codec: "libmp3lame", args: ["-q:a", "2"] },  { ext: "aac", mime: "audio/aac", label: "AAC", codec: "aac", args: [] },  { ext: "wav", mime: "audio/wav", label: "WAV", codec: "pcm_s16le", args: [] },  { ext: "ogg", mime: "audio/ogg", label: "OGG", codec: "libvorbis", args: ["-q:a", "4"] },  { ext: "flac", mime: "audio/flac", label: "FLAC", codec: "flac", args: [] },];export default function AudioExtractPanel({ video }: AudioExtractPanelProps) {  const { init, run, cancel, progress, log, loaded, loading, error, cancelling, running } = useFFmpeg();  const { t } = useTranslation();  const [format, setFormat] = useState(FORMATS[0]);  const [outputUrl, setOutputUrl] = useState<string | null>(null);
  const [outputBlob, setOutputBlob] = useState<Blob | null>(null);  useEffect(() => { init(); }, [init]);  const handleExtract = useCallback(async () => {    setOutputUrl(null);    await run(async (instance) => {      if (!video.data) throw new Error("No video data loaded");      const ext = video.name.match(/\.[^.]+$/)?.[0] || ".mp4";      const inputName = "input" + ext;      const outputName = `audio.${format.ext}`;      await instance.writeFile(inputName, video.data);      await instance.exec(["-i", inputName, "-vn", "-c:a", format.codec, ...format.args, "-y", outputName]);      const raw = await instance.readFile(outputName);      const blob = new Blob([raw as BlobPart], { type: format.mime });      setOutputBlob(blob);
      setOutputUrl(URL.createObjectURL(blob));
    });
  }, [video.data, video.name, format, run]);
  return (    <div className="card space-y-6">      <div>        <label className="block text-sm text-surface-400 mb-2">{t("audio.format")}</label>        <div className="flex gap-2">          {FORMATS.map((f) => (            <button key={f.ext} onClick={() => setFormat(f)}              className={`px-4 py-2 rounded-lg text-sm font-medium transition-colors ${format.ext === f.ext ? "bg-brand-600 text-white" : "bg-surface-800 text-surface-300 hover:bg-surface-700"}`}>              {f.label}            </button>          ))}        </div>      </div>      <ProcessingOverlay active={running} progress={progress} label={t("audio.extracting")} log={log} onCancel={cancel} cancelling={cancelling} />      {error && <div className="banner-error">{error}</div>}      <div className="flex gap-3">        <button onClick={handleExtract} disabled={loading || running} className="btn-primary">          {!loaded ? t("common.load_ffmpeg") : t("audio.extract")}        </button>        {outputUrl && outputBlob && <DownloadButton blob={outputBlob} filename={video.name.replace(/\.[^.]+$/, "") + "." + format.ext} filters={[{ name: format.label, extensions: [format.ext] }]} label={t("common.download") + " " + format.label} />}      </div>    </div>  );}